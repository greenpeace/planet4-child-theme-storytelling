{% extends "base.twig" %}

{% block content %}
	<div class="logo-container">
		<div class="content animated fadeIn">
			<div class="logos">
				{% include 'blocks/whale_logo.twig' %}
				{% include 'blocks/leaf_logo.twig' %}
				{% include 'blocks/gecko_logo.twig' %}
				{% include 'blocks/cloud_logo.twig' %}
				{% include 'blocks/fish_logo.twig' %}
			</div>
			{% if fn('is_front_page') %}
				<h2>{{ __( 'Write your Story. Change the World.', 'planet4-child-theme-storytelling' ) }}</h2>
			{% else %}
				<h2>{{ post.title }}</h2>
			{% endif %}
		</div>
	</div>
	<div class="page-template">
		{{ post.content|raw }}
	</div>
{% endblock %}
